# Copy sang ".env" khi bắt đầu dùng database thật (Giai đoạn 2 trở đi).
# GIAI ĐOẠN 1: KHÔNG cần file này — app chạy bằng dữ liệu mẫu.

# PostgreSQL (dùng chung server DB trên VPS, mỗi app 1 database riêng)
DATABASE_URL="postgresql://postgres:biglight0812@localhost:5432/biglight_management_db?schema=public"
